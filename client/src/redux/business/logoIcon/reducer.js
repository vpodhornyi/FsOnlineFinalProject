import {HOME_ROUTE} from "../../../utils/constants";

const testColor = '#000000';

const INIT = {
  logo: {
    iconName: "Twitter",
    href: HOME_ROUTE
  }
};

export default (state = INIT) => state;
